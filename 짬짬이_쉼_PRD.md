# 짬짬이 쉼 MVP 제품 요구사항 정의서(PRD)

- **버전:** 1.0
- **형태:** 정적 웹 MVP
- **배포:** GitHub Pages (저장소명 제안: `jjam-rest`)

## 핵심 정의

| 항목 | 내용 |
|---|---|
| 서비스명 | 짬짬이 쉼 |
| 핵심 사용자 | 전자칠판을 사용하는 초등학교 교사 |
| 핵심 상황 | 체육·점심 직후, 소란한 반, 수업 시작 전 1~5분 |
| 핵심 가치 | 들뜬 반을 몇 분 만에 가라앉힌다. 교사도 같이 쉰다. |
| 핵심 흐름 | 유형 선택 → 시간 선택 → 시작 → **자동 진행** → 마무리 |
| MVP 범위 | 5개 유형, 15~20편, 1·3·5분 모드, 완전 자동 진행, 합성 배경음 |

**자매 프로젝트와의 관계** — 짬짬이 게임(105종)·퀴즈(101종)·영상(425편)·이야기(100편)는
모두 에너지를 올리거나 유지하는 활동입니다. 쉼은 가족 중 유일하게 **에너지를 내리는**
활동입니다. 몸을 움직이는 활동은 짬짬이 스트레칭이 따로 있으므로 다루지 않습니다.

## 1. 제품 개요

### 1.1 한 문장 정의
교사가 쉼 유형과 시간을 고르고 시작을 누르면, 화면이 호흡 안내·소리·상상 문구를
스스로 진행해 학생들을 차분하게 만드는 전자칠판용 웹서비스입니다.
**시작 버튼 이후 교사는 아무것도 누르지 않습니다.**

### 1.2 해결하려는 문제
- 체육·점심·쉬는 시간 직후 들뜬 반을 가라앉히는 데 수업 시간을 잃습니다.
- "조용히 하세요"를 반복하는 것 말고는 마땅한 도구가 없습니다.
- 명상·호흡 앱은 어른용이거나, 로그인·유료·영어라서 교실에서 바로 못 씁니다.
- 기존 짬짬이 활동(게임·퀴즈·이야기)은 모두 교사가 화면 앞에서 진행해야 합니다.
  쉼만큼은 교사도 손을 떼고 함께 쉬어야 합니다.

### 1.3 목표
- 접속 후 10초 안에 활동 시작
- 시작 후 교사 조작 횟수 0회
- 1·3·5분 안에 실제 종료
- 소리 없이 화면만으로도 완결 (스피커 고장·음소거 교실 대응)
- 활동이 끝나면 교실이 시작 전보다 조용하다

### 1.4 비목표
- 스트레칭·율동 등 몸을 움직이는 활동 (짬짬이 스트레칭 영역)
- 수면 유도·본격 명상 수련
- 학생 개인 기기 사용, 계정, 기록 저장
- 심리 상태 측정·평가
- 음원 파일 스트리밍 (아래 소리 설계 참고 — 파일 자체를 두지 않음)

## 2. 사용자와 환경

### 주요 사용자
- 1차: 초등학교 교사
- 2차: 초등학생 (전 학년 — 쉼은 학년 구분이 필요 없음)

### 사용 환경
- 전자칠판, 대형 TV, 프로젝터 + 교실 스피커
- Chrome·Edge 등 최신 브라우저
- 학생 개인 기기나 계정 없이 전체 참여
- 정적 파일로 실행, 오프라인 동작

## 3. 콘텐츠 체계

### MVP 쉼 유형 (5종)
1. **🫁 숨 고르기** — 커졌다 작아지는 원을 따라 숨쉬기. 상자 호흡(4-4-4-4),
   4-6 호흡(들숨 4초·날숨 6초), 한 손가락 산 오르기 등 패턴 3~4개
2. **🌧 소리 여행** — 눈 감고 소리만 듣기. 빗소리·파도·바람·모닥불 (전부 합성음)
3. **🎈 상상 여행** — 화면 문장이 천천히 넘어가는 상상 안내.
   "구름 위에 누웠다고 상상해요 …" (한 화면에 한 문장)
4. **🧊 몸 힘 빼기** — 앉은 채로 어깨·손·얼굴에 힘을 꽉 주었다 빼기 (이완이지 움직임이 아님)
5. **💗 마음 보기** — 오늘 고마웠던 것 하나, 지금 내 기분 하나를 조용히 떠올리기

각 유형 3~4편, 총 15~20편으로 시작합니다.

### 작성 기준
- 문구는 저학년도 읽는 짧은 한 문장씩, 화면에 하나만
- 명령형 대신 청유형 ("힘을 빼 볼까요")
- 날숨을 들숨보다 길게 (진정 호흡의 기본)
- 소리 없이 화면만 봐도 진행이 이해되도록 애니메이션·문구를 함께 설계

### 안전 기준
- 종교적 표현·기도·만트라 금지
- 수면 유도·최면형 문구 금지 ("잠이 듭니다" ✗ → "몸이 편안해집니다" ○)
- 무섭거나 슬픈 상상 소재 금지 (어둠 속 혼자, 이별 등)
- 빠른 점멸·급격한 화면 전환 금지 — 느린 애니메이션만 (광과민성 발작 예방)
- `prefers-reduced-motion` 설정 시 애니메이션을 페이드로 대체

## 4. 핵심 흐름

```text
홈(유형 선택) → 시간 선택(1·3·5분) → [시작] → 자동 진행 (교사 조작 없음) → 마무리 화면
```

### 시간별 진행 (예: 숨 고르기)
- **1분:** 준비 문구 10초 → 호흡 4~5회 → 마무리 문구
- **3분:** 준비 20초 → 호흡 1분 → 소리와 함께 호흡 1분 → 천천히 눈 뜨기 → 마무리
- **5분:** 준비 → 호흡 → 몸 힘 빼기 한 부위 → 호흡 → 마음 보기 한 가지 → 마무리

각 세션의 단계·초는 데이터(`sessions.json`)에 정의하고, 화면은 그것을 재생만 합니다.

## 5. 화면
1. 홈 — 유형 5개 카드
2. 시간 선택 — 1·3·5분 (마지막 선택 기억)
3. 진행 — 전체 화면급 단순함: 애니메이션 + 문장 한 줄 + 남은 시간(작게)
4. 마무리 — "이제 시작할 준비가 됐어요" 한 문장, [한 번 더]·[처음으로]

진행 화면에는 **일시정지 버튼 하나만** 둡니다(교내 방송·손님 대응).
누르면 소리·애니메이션·타이머가 함께 멈춥니다.

## 6. 기능 요구사항

| ID | 기능 | 우선순위 |
|---|---|---|
| FR-01 | 유형·시간 선택 후 완전 자동 진행 (**시작 후 교사 조작 0회**) | Must |
| FR-02 | 단계별 문구·애니메이션·소리를 데이터 정의대로 재생 | Must |
| FR-03 | 일시정지·재개 (소리·화면·타이머 동시) | Must |
| FR-04 | Web Audio 합성 배경음 + 음소거 토글 (localStorage 기억) | Must |
| FR-05 | 남은 시간 표시 (교사용, 작게) | Must |
| FR-06 | 같은 유형 다른 편 즉시 교체 | Should |
| FR-07 | 최근 사용 기억·홈에 표시 | Should |
| FR-08 | 오프라인 실행 (SW) | Should |
| FR-09 | `prefers-reduced-motion` 대응 | Must |
| FR-10 | 반응형 UI (전자칠판 우선) | Must |

## 7. 데이터 구조

`data/sessions.json` 한 파일이 콘텐츠의 단일 소스입니다. 완전한 예시 1편:

```json
{
  "id": "breath-001",
  "title": "네모 호흡",
  "type": "breath",
  "durations": [1, 3, 5],
  "sound": "rain",
  "steps": [
    { "phase": "ready",  "text": "허리를 펴고 앉아요. 두 손은 무릎 위에.", "seconds": 10, "anim": "none" },
    { "phase": "breath", "text": "네모를 따라 숨 쉬어요.",               "seconds": 60, "anim": "box-4444" },
    { "phase": "breath", "text": "이번엔 소리를 들으며 계속해요.",        "seconds": 60, "anim": "box-4444" },
    { "phase": "close",  "text": "천천히 눈을 뜨고 앞을 봐요.",           "seconds": 10, "anim": "fade" }
  ],
  "closing": "마음이 잔잔해졌어요. 이제 시작할 준비가 됐어요."
}
```

- `durations`에 따라 `steps`를 자르거나 반복하는 규칙은 진행 로직(8절)에 둡니다.
- `anim`은 미리 정의한 애니메이션 키(`box-4444`, `circle-46`, `fade`, `none` 등)만 씁니다.
- `sound`는 합성음 프리셋 키(`rain`, `wave`, `wind`, `fire`, `none`)만 씁니다.
- 로컬에는 음소거 설정·최근 사용·마지막 시간 선택만 저장합니다. 학생 정보는 없습니다.

## 8. 진행 로직
- 상태: `HOME → SETUP → PLAYING → (PAUSED) → DONE`
- `PLAYING`은 steps 배열을 순서대로 소비하는 타임라인 하나로 구현 (단계 조작 API 없음)
- 1분 모드는 `phase:"breath"` 계열 중 첫 단계만, 5분 모드는 전체 + 반복으로 채움
- 최근 사용한 편은 같은 유형에서 다시 뽑을 때 제외

## 9. 소리 설계 (중요)

**음원 파일을 두지 않습니다.** 짬짬이 게임의 `shared/engine.js`가 배경음악을
Web Audio(oscillator·gain 합성)로 만드는 것과 같은 방식으로, 쉼의 자연음도 합성합니다.

- 빗소리 = 필터를 거친 화이트 노이즈, 파도 = 느린 LFO를 건 노이즈,
  바람 = 밴드패스 노이즈 스윕, 패드 = 디튠한 사인파 2~3개
- 장점: 저작권 문제 0, 용량 0, 오프라인 완전 동작, 길이 무제한
- 브라우저 자동재생 정책은 "[시작] 클릭 후 재생"이므로 문제없습니다
- 소리는 항상 보조입니다 — 음소거 상태에서도 활동이 완결되어야 합니다 (1.3 목표)

## 10. 기술 구성
- HTML, CSS, Vanilla JavaScript — 프레임워크·빌드 없음 (가족 공통)
- `data/sessions.json` + LocalStorage — 서버·DB 없음
- Service Worker (network-first) + PWA manifest — 오프라인
- GitHub Pages (main 직접 서빙)

### 가족 공통 인프라 합류 (구현 세션이 반드시 따를 것)

짬짬이 게임(jjam)·퀴즈(jjam-quiz)·영상(jjam-video)·이야기(jjam-story)가 이미 쓰는 관례입니다.

| 항목 | 내용 |
|---|---|
| 브랜드 색 | **티일/청록 계열** (예: `#0E7C86` 안팎에서 시안 후 확정). 기존 넷: 게임 앰버 `#FFB703` · 퀴즈 그린 `#12A57C` · 영상 스카이 `#4FA8E8` · 이야기 퍼플 `#6145B5` |
| 아이콘 | **같은 틀 + 다른 알맹이** — 둥근 사각 타일 + 우상단 배지(흰 원 + 네이비 시계)는 넷과 동일, 바탕색(티일)과 가운데 심볼만 쉼 고유. `favicon.svg` 한 파일이 탭·헤더·앱 아이콘의 단일 소스. PNG는 jjam 저장소의 `scripts/gen-icons.mjs`로 생성 |
| 헤더 | 색 채운 띠(티일) 위에 [로고 46px + 제목 + 부제], 본문과 같은 max-width로 가운데 정렬 (화면 끝까지 늘리지 않음). 헤더 아래 흰 안내 띠 한 줄 |
| 자매 바로가기 | `shared/jjam-switcher.js` — **jjam이 상류.** jjam의 SITES 배열에 쉼을 추가·머지한 뒤 각 저장소에서 sync-shared로 받음. 쉼 저장소도 `scripts/sync-shared.mjs` + `.github/workflows/shared-sync.yml`(매일 05:30 KST 드리프트 확인)을 갖출 것 |
| 웹폰트 | Pretendard 서브셋 자가 호스팅 (`assets/fonts/`, sync-shared 대상). 새 문구를 넣으면 `scripts/check-font-coverage.mjs`로 서브셋 커버리지 확인 |
| 검증·CI | `scripts/validate-data.mjs`(sessions.json이 화면 상수와 맞는지, 안전 기준 위반 문구 검사) + `check-font-coverage.mjs`를 CI `verify` 게이트로. 화면 상수는 앱 코드에서 읽어 와 상수·데이터가 어긋나면 실패하게 |
| PR 워크플로 | PR 생성 → CI 통과 시 squash 머지 (가족 공통 사용자 지시) |

## 11. MVP 필수 범위
- 5개 유형, 15~20편
- 1·3·5분 모드
- 완전 자동 진행 + 일시정지
- 합성 배경음 4종 + 음소거
- 호흡 애니메이션 2종 이상 (원·네모)
- 최근 사용 기억
- 반응형 UI, 오프라인

## 12. 완료 기준
- 홈에서 10초 안에 활동 시작
- **시작 후 마무리까지 화면을 한 번도 누르지 않고 완주**
- 1·3·5분 모드가 선언한 시간 ±5초 안에 종료
- 일시정지 시 소리·화면·타이머가 함께 멈추고 함께 재개
- 음소거 상태로도 전 과정이 이해됨
- `prefers-reduced-motion`에서 점멸·급전환 없음
- 전자칠판 뒷자리에서 문구가 읽힘 (본문 32px 상당 이상)
- GitHub Pages에서 오류 없이 실행, 오프라인 재방문 동작

## 13. 개발 순서
1. sessions.json 스키마 + 숨 고르기 3편으로 세로 관통 (홈→자동 진행→마무리)
2. 호흡 애니메이션 (CSS/SVG, reduced-motion 대체 포함)
3. Web Audio 합성음 4종 + 음소거
4. 나머지 4유형 콘텐츠 채우기 (15~20편)
5. validate-data + font-coverage + CI
6. 아이콘(같은 틀+티일) 시안 → 확정 → gen-icons
7. SW·manifest·전자칠판 반응형 점검
8. jjam 상류에 SITES 추가 → 5개 저장소 sync-shared
9. GitHub Pages 배포, 교실 테스트

## 14. 제품 원칙

> **교사도 같이 쉰다. 화면이 알아서 진행하고, 끝나면 교실이 차분하다.**

모든 기능은 `10초 안에 시작` · `시작하면 손 뗀다` · `끝나면 차분한 교실` 원칙을 따릅니다.
